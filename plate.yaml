name: elk-stack
owner: kikplate-public
description: Elasticsearch Logstash and Kibana 8 stack for log aggregation and search, persistent named volumes, health checks, and bridge network isolation.
version: 1.0.0
category: observability

tags:
  - elasticsearch
  - logstash
  - kibana
  - elk
  - logging
  - observability
  - docker-compose

schema:
  projectName:
    type: string
    default: elk
  elasticVersion:
    type: string
    default: "8.14.0"
  esPort:
    type: int
    default: 9200
  kibanaPort:
    type: int
    default: 5601
  logstashBeatsPort:
    type: int
    default: 5044
  logstashTcpPort:
    type: int
    default: 5000
  esJavaOpts:
    type: string
    default: "-Xms512m -Xmx512m"
  lsJavaOpts:
    type: string
    default: "-Xms256m -Xmx256m"
  esClusterName:
    type: string
    default: docker-cluster
  esReplicaCount:
    type: int
    default: 0

files:
  - path: docker-compose.yml
    template: templates/docker-compose.yml.tmpl
  - path: logstash.conf
    template: templates/logstash.conf.tmpl
  - path: .env
    template: templates/.env.tmpl
  - path: README.md
    template: templates/README.md.tmpl
