name: mysql
owner: kikplate-public
description: MySQL 8 database with optional phpMyAdmin web interface, persistent named volumes, health checks, and bridge network isolation.
version: 1.0.0
category: database

tags:
  - mysql
  - database
  - phpmyadmin
  - docker-compose

schema:
  projectName:
    type: string
    default: mysql
  mysqlVersion:
    type: string
    default: "8.4"
  mysqlUser:
    type: string
    default: app
  mysqlDb:
    type: string
    default: app
  mysqlPort:
    type: int
    default: 3306
  mysqlCharset:
    type: string
    default: utf8mb4
  mysqlCollation:
    type: string
    default: utf8mb4_unicode_ci
  phpmyadminVersion:
    type: string
    default: "5"
  phpmyadminPort:
    type: int
    default: 8080

modules:
  phpmyadmin:
    enabled: true

files:
  - path: docker-compose.yml
    template: templates/docker-compose.yml.tmpl
  - path: .env
    template: templates/.env.tmpl
  - path: README.md
    template: templates/README.md.tmpl
