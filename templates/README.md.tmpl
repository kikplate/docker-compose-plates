# {{ .projectName }}

Redis in-memory data store with Redis Commander web interface.

## Quick Start

    # .env is generated — update passwords before starting
    docker compose up -d

## Access

* Redis: localhost:{{ .redisPort }}
* Redis Commander: http://localhost:{{ .commanderPort }}
