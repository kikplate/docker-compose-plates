# {{ .projectName }}

MongoDB document database with Mongo Express web interface.

## Quick Start

    cp .env.example .env
    docker compose up -d

## Access

* MongoDB: localhost:{{ .mongoPort }}
* Mongo Express: http://localhost:{{ .mongoExpressPort }}
