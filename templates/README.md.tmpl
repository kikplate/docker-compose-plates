# {{ .projectName }}

RabbitMQ AMQP message broker with management UI.

## Quick Start

    # .env is generated — update passwords before starting
    docker compose up -d

## Access

* AMQP: localhost:{{ .amqpPort }}
* Management UI: http://localhost:{{ .managementPort }}

Log in to the management UI with the credentials set in your .env file.
